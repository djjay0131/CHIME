# Design Docs

This directory holds practical code-reading guides for CHIME.

- `codebase-guide.md`: high-level map of the repository, core runtime components, build-time feature flags, and experiment flow.
